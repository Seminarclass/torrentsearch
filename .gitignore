# data/latest.json IS committed — generated by scrape and refreshed by Actions.
# Local-only artifacts (logs, generated during dev) should be ignored, not the cache.
data/*.log
*.log
.env
.DS_Store
dist/
